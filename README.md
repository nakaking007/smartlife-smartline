# SmartLifeScheduler

## วิธีส่งไฟล์ขึ้นระบบเดิม

1. รวมไฟล์ทั้งหมดลงในโฟลเดอร์ `SmartLifeScheduler`
2. บีบอัดเป็น ZIP

```bash
zip -r SmartLifeScheduler.zip SmartLifeScheduler/
```

3. อัปโหลดเข้าสู่ระบบเดิมผ่านหน้าจัดการที่เคยใช้ เช่น ระบบที่เชื่อมกับ LINE หรือ Google Drive/Server
4. ติดตั้ง dependencies

```bash
npm install
```

5. สร้างผู้ใช้ฐานข้อมูล

```bash
mongo < createSmartlife.js
mongo < createAdmin.js
```

หากเครื่องใช้ MongoDB รุ่นใหม่ ให้ใช้ `mongosh` แทน `mongo`

```bash
mongosh < createSmartlife.js
mongosh < createAdmin.js
```

6. รันระบบ

```bash
node server.js
```

7. ทดสอบใน LINE ระบบจะส่งข้อความแจ้งเตือนอัตโนมัติทุกเช้าเวลา 06:00 ตามเวลา `Asia/Bangkok`

## เมนูแก้ไขนัดหมาย

ผู้ใช้สามารถพิมพ์ใน LINE:

```text
เมนูแก้ไข
```

ระบบจะแสดงรายการนัดหมาย พร้อมปุ่ม quick reply สำหรับ `แก้` และ `ลบ`

ถ้าต้องการแผงแก้ไขแบบหน้าเว็บ ให้เปิด:

```text
http://localhost:3000/appointments-panel
```

แผงนี้จะแสดงนัดหมายทั้งหมดที่ยังไม่ถูกลบ พร้อมปุ่ม `แก้ไข`, `บันทึก`, และ `ลบ` ทุกรายการ การแก้ช่องวันเวลาในแผงนี้จะบันทึกและแสดงผลตามเวลาจริงที่ผู้ใช้เลือก โดยตีความเป็นเวลาไทย `Asia/Bangkok` แบบ 24 ชั่วโมง เช่น หากเลือก `15.00 น.` ระบบจะแสดง `15.00 น.` ไม่แปลงเป็น `22.00 น.`

คำสั่งแก้ไขโดยตรง:

```text
แก้นัดหมาย <ID> | ชื่อใหม่ | 2026-05-28 09.00 น. | สถานที่ | ชุด
```

สามารถใช้รูปแบบวันเวลาแบบไทยได้ด้วย:

```text
แก้นัดหมาย <ID> | ชื่อใหม่ | 28-05-2569 : 09.00 น. | สถานที่ | ชุด
```

ตัวอย่าง:

```text
แก้นัดหมาย 6a0fe46ad16ce9833c650bd0 | ประชุมทีม SmartLife | 2026-05-28 10.00 น. | ห้องประชุม | ชุดสุภาพ
```

ตัวอย่างแบบไทย:

```text
แก้นัดหมาย 6a0fe46ad16ce9833c650bd0 | ประชุมทีม SmartLife | 28-05-2569 : 10.00 น. | ห้องประชุม | ชุดสุภาพ
```

คำสั่งลบนัดหมายซ้ำ:

```text
ลบนัดหมาย <ID>
```

หลังจากเปิด `เมนูแก้ไข` หรือ `รายงานวันนี้` สามารถใช้เลขลำดับได้ด้วย:

```text
แก้นัดหมาย 1
ลบนัดหมาย 1
```

คำสั่งตรวจนัดหมายที่อาจซ้ำ:

```text
นัดหมายซ้ำ
```

ระบบจะเปลี่ยนสถานะนัดหมายที่ลบเป็น `deleted` เพื่อป้องกันการลบถาวรโดยไม่ตั้งใจ

## คำสั่งใน LINE

### คำสั่งเร็วแบบ /

| คำสั่ง | ผลลัพธ์ที่ได้ |
| --- | --- |
| `/แบบฟอร์ม` | แสดงตัวเลือกแบบฟอร์มสมัครสมาชิก, แบบฟอร์มนัดหมาย และแผงแก้ไขนัดหมาย |
| `/สภาพอากาศ` | แสดงพยากรณ์อากาศ ฝน ลม/พายุ ฝุ่น PM2.5 อุณหภูมิ พร้อมตีค่าระดับ |
| `/นัดหมาย` | แสดงตารางนัดหมายทั้งหมด พร้อมปุ่มแก้ไข/ลบหลังรายการ |
| `/ปฏิทิน` | เปิด SmartLife Calendar แบบ LIFF สำหรับดู เพิ่ม ลบ นัดหมาย และจัดการ To-do ใน LINE |
| `/ภัยพิบัติ` | แสดงแผ่นดินไหว พายุ สึนามิ ในเอเชีย พร้อมระดับความรุนแรงและระยะจากไทย |
| `แผ่นดินไหวล่าสุด` | ประเมินเหตุแผ่นดินไหวล่าสุดจาก USGS พร้อมระดับระบบ ระยะจาก กทม. และบันทึกประวัติ |
| `/พรุ่งนี้` | แสดงนัดหมายพรุ่งนี้ |
| `/สัปดาห์นี้` | แสดงนัดหมายสัปดาห์นี้ |
| `/เดือนนี้` | แสดงนัดหมายเดือนนี้ |
| `/สร้างภาพ` | ถามรายละเอียดภาพ รับภาพต้นฉบับ เขียนพร้อมท์ภาษาไทยขนาด 1:1 ให้ตรวจ แล้วจึงสร้างพร้อมปุ่มดาวน์โหลด |
| `/คำถาม`, `/คำถามอื่น` | เข้าโหมดถามตอบ AI ด้วย provider ฟรี/สำรองที่ตั้งค่าไว้ |
| `/แปลภาษา` | เข้าโหมดแปลไทยเป็นอังกฤษ หรืออังกฤษเป็นไทย |
| `/สมัคร` | เปิดฟอร์มสมัคร พร้อมข้อมูลแพ็กเกจและช่องทางโอนจ่ายตามที่ตั้งค่าไว้ |
| `/ปลดลอค` | แสดงแพ็กเกจ ฟรี/Plus/VIP และคำสั่งแอดมินปลดล็อก |
| `/บริการฉุกเฉิน` | รวมเบอร์ช่วยเหลือประชาชนและหน่วยงานสำคัญ เช่น 191, 1669, 199, 1784, 1418 |
| `/ตรวจเช็ค` | ตรวจสถานะคำสั่ง LINE, AI, สร้างภาพ, ฟอร์ม และค่าที่ต้องตั้งเพิ่ม |

ลิงก์ฟอร์มแยก:

- `/forms`: หน้าเลือกแบบฟอร์มทั้งหมด
- `/register-form`: แบบฟอร์มสมัครสมาชิกเท่านั้น
- `/appointment-form`: แบบฟอร์มนัดหมายเท่านั้น
- `/appointments-panel`: แผงดู/แก้/ลบนัดหมายทั้งหมด
- `/liff/calendar`: หน้า SmartLife Calendar สำหรับเปิดผ่าน LIFF หรือ browser
- `/todos`: API สำหรับ To-do เช่น เพิ่มงาน ติ๊กเสร็จ เปิดใหม่ และลบ
- `/earthquake-warnings`: API ประวัติการประเมินแผ่นดินไหวล่าสุด

| คำสั่ง | วิธีพิมพ์ | ผลลัพธ์ที่ได้ |
| --- | --- | --- |
| เปิดคู่มือ | `คู่มือ` | แสดงคำอธิบายระบบและรายการคำสั่งที่ใช้ได้ |
| เปิดคู่มือ | `วิธีใช้` | แสดงคำอธิบายระบบและรายการคำสั่งที่ใช้ได้ |
| เปิดแผงหลัก | `เมนูหลัก` | แสดงปุ่มไปยังเมนูแก้ไขนัดหมาย รายงานวันนี้ และคู่มือ |
| เปิดแผงหลัก | `แผงหลัก` | แสดงปุ่มไปยังเมนูแก้ไขนัดหมาย รายงานวันนี้ และคู่มือ |
| เปิดแผงนัดหมายเว็บ | `/appointments-panel` | ดูนัดหมายทั้งหมด แก้ไข บันทึก หรือลบทีละรายการ |
| เปิดเมนูแก้ไข | `เมนูแก้ไข` | แสดงรายการนัดหมาย พร้อมปุ่ม `แก้` และ `ลบ` |
| เปิดเมนูแก้ไข | `แก้ไข` | แสดงรายการนัดหมาย พร้อมปุ่ม `แก้` และ `ลบ` |
| เปิดเมนูแก้ไข | `แก้ไขนัดหมาย` | แสดงรายการนัดหมาย พร้อมปุ่ม `แก้` และ `ลบ` |
| แก้นัดหมายโดยตรง | `แก้นัดหมาย <ID> \| ชื่อใหม่ \| 2026-05-28 09.00 น. \| สถานที่ \| ชุด` | แก้ชื่อนัดหมาย วันเวลา สถานที่ และชุด |
| แก้เวลาอย่างเดียว | `แก้เวลา <ID> <เวลาจริง>` เช่น `แก้เวลา <ID> 15.00 น.` | แก้เฉพาะเวลา โดยยึดวันเดิมของนัดหมายนั้นและแสดงเวลาจริงที่บันทึก |
| แก้นัดหมายแบบวันไทย | `แก้นัดหมาย <ID> \| ชื่อใหม่ \| 28-05-2569 : 09.00 น. \| สถานที่ \| ชุด` | แก้วันเวลาโดยใช้รูปแบบไทย ปี พ.ศ. |
| บันทึกนัดหมายในแชต | `บันทึกนัดหมาย \| ชื่อ \| 28-05-2569 : 15.00 น. \| สถานที่ \| ชุด` | เพิ่มนัดหมายจาก LINE โดยตรง หากเปิดฟอร์มเว็บไม่ได้ |
| ลบนัดหมาย | `ลบนัดหมาย <ID>` | เปลี่ยนสถานะนัดหมายเป็น `deleted` |
| แก้ตามเลขลำดับ | `แก้นัดหมาย 1` | เลือกรายการที่ 1 จากเมนูล่าสุด แล้วให้ส่งข้อมูลใหม่ |
| ลบตามเลขลำดับ | `ลบนัดหมาย 1` | ลบรายการที่ 1 จากเมนูล่าสุด |
| ตรวจนัดหมายซ้ำ | `นัดหมายซ้ำ` | แสดงกลุ่มนัดหมายที่มีแนวโน้มซ้ำ พร้อม ID สำหรับลบ |
| ดูรายงานวันนี้ | `รายงาน` | แสดงตารางนัดหมายวันนี้ พร้อม ID สำหรับแก้ไข/ลบ |
| ดูรายงานวันนี้ | `รายงานวันนี้` | แสดงตารางนัดหมายวันนี้ พร้อม ID สำหรับแก้ไข/ลบ |
| ดูตารางวันนี้ | `ตารางวันนี้` | แสดงตารางนัดหมายวันนี้ พร้อม ID สำหรับแก้ไข/ลบ |
| ดูภาวะฉุกเฉิน | `ภาวะฉุกเฉิน` | แสดง alert เร่งด่วนที่ยัง active |
| ดูภาวะเร่งด่วน | `เร่งด่วน` | แสดง alert เร่งด่วนที่ยัง active |
| ดูภัยพิบัติ | `ภัยพิบัติ` | แสดงรายงานพายุ แผ่นดินไหว สึนามิ น้ำท่วม และ alert เร่งด่วน |
| ดูพายุ | `พายุ` | แสดงรายงานพายุที่ยัง active |
| ดูแผ่นดินไหว | `แผ่นดินไหว` | แสดงรายงานแผ่นดินไหวที่ยัง active |
| ดูสึนามิ | `สึนามิ` | แสดงรายงานสึนามิที่ยัง active |
| ดูน้ำท่วม | `น้ำท่วม` | แสดงรายงานน้ำท่วมที่ยัง active |
| ดูอากาศจากตำแหน่งจริง | ส่งโลเคชันใน LINE | แสดงอุณหภูมิ ฝน ลม/พายุ PM2.5 และคำประเมินจากพิกัดที่ส่งมา |
| ถามฝน | `วันนี้ฝนจะตกไหม` | ตอบโอกาสฝน 12 ชั่วโมงข้างหน้าและฝนล่าสุด 1 ชั่วโมง |
| ถามอุณหภูมิ | `วันนี้อุณหภูมิสูงสุดเท่าไร` | ตอบอุณหภูมิสูงสุดวันนี้ |
| ถามฝุ่น | `วันนี้ฝุ่นระดับใด` | ตอบค่า PM2.5 และระดับฝุ่น |
| ถามอากาศหนาว | `วันนี้หนาวไหม` | ตอบว่าอากาศหนาวหรือไม่ |
| ถามอากาศร้อน | `วันนี้ร้อนไหม` | ตอบว่าอากาศร้อนหรือไม่ |
| สร้างคำกล่าว | `คำกล่าว <ชื่องาน>` | สร้างร่างคำกล่าวสุภาพสำหรับพิธีนั้น |
| สร้างคำกล่าว | `เขียนคำกล่าว <ชื่องาน>` | สร้างร่างคำกล่าวสุภาพสำหรับพิธีนั้น |

### วิธีแก้ไขจากปุ่ม

1. กดปุ่ม `แก้ไข` บนการ์ดนัดหมายใน LINE
2. ระบบจะตอบกลับให้ส่งข้อมูลใหม่
3. หากต้องการแก้เฉพาะเวลา ให้พิมพ์เวลาจริงที่ต้องการได้ทันที ตัวอย่างรูปแบบ:

```text
15.00 น.
```

หรือพิมพ์ข้อมูลครบตามรูปแบบ:

```text
ชื่อใหม่ | 28-05-2569 : 09.00 น. | สถานที่ | ชุด
```

ตัวอย่าง:

```text
ประชุมทีม SmartLife | 28-05-2569 : 10.00 น. | ห้องประชุมใหญ่ | ชุดสุภาพ
```

ผลลัพธ์: ระบบจะแก้นัดหมายรายการที่กดไว้ และตอบกลับว่านัดหมายถูกแก้ไขเรียบร้อยแล้ว

### วิธีลบจากปุ่ม

1. กดปุ่ม `ลบ` บนการ์ดนัดหมายใน LINE
2. ระบบจะเปลี่ยนสถานะนัดหมายนั้นเป็น `deleted`
3. ระบบตอบกลับว่า:

```text
เรียน นายท่าน นัดหมายนี้ถูกลบเรียบร้อยแล้วค่ะ
```

หมายเหตุ: ระบบเป็นการลบแบบ soft delete เพื่อป้องกันการลบถาวรโดยไม่ตั้งใจ

### รูปแบบวันเวลาที่รองรับ

ระบบรองรับทั้งรูปแบบสากลและรูปแบบไทย:

```text
2026-05-28 09.00 น.
28-05-2569 : 09.00 น.
28/05/2569 : 09.00 น.
28/05/2569 : 24.00 น.
```

ถ้าใช้ปี พ.ศ. เช่น `2569` ระบบจะแปลงเป็น ค.ศ. `2026` ให้อัตโนมัติ

ระบบตีความวันเวลาที่ผู้ใช้พิมพ์และค่าที่ส่งผ่าน API เป็นเวลาไทย `Asia/Bangkok` และแสดงผลแบบ 24 ชั่วโมงพร้อม `น.` เสมอ แม้ข้อความจะมี `Z` หรือ offset ติดมา จนกว่าจะมีการเพิ่มสถานะเดินทางต่างประเทศในอนาคต ถ้าพิมพ์ `24.00 น.` ระบบจะถือเป็นเที่ยงคืนสิ้นวันนั้น

## คำกล่าวในพิธี

ผู้ใช้สามารถให้ SmartLife สร้างร่างคำกล่าวพิธีต่างๆ ได้ โดยพิมพ์:

```text
คำกล่าว <ชื่องาน>
```

ตัวอย่าง:

```text
คำกล่าว เปิดงานประชุมผู้ปกครอง
```

ระบบจะสร้างร่างคำกล่าวสุภาพ มีบริบทของงาน คำเปิด คำขอบคุณ และคำลงท้ายให้ทันที

## โครงสร้างการแจ้งเตือนเวลา 06:00

ทุกเช้าเวลา 06:00 น. ระบบจะแจ้งเองไปที่ LINE โดยรายงานสภาพอากาศและตารางนัดหมาย ไม่ต้องให้ผู้ใช้พิมพ์ถามก่อน

ถ้าเครื่องหรือ server เพิ่งเปิดหลัง 06:00 แต่ยังไม่ถึง 12:00 ระบบจะส่งรายงานเช้าชดเชยครั้งเดียว และบันทึกสถานะใน collection `cron_runs` เพื่อไม่ส่งซ้ำในวันเดียวกัน

ข้อมูลอากาศที่รายงานทุกเช้า:

- อุณหภูมิสูงสุด
- โอกาสฝน 12 ชั่วโมงข้างหน้า
- ฝนล่าสุด 1 ชั่วโมง
- ฝุ่น PM2.5 จาก Air Pollution API เมื่อมีข้อมูล

ข้อความจะเรียงลำดับดังนี้:

- ระบบเรียก `sendMorningGreeting()`
- คำทักทายขึ้นต้นด้วย `เรียน นายท่าน`
- รายงานอากาศ: อุณหภูมิสูงสุด, โอกาสฝน 12 ชั่วโมง, ฝนล่าสุด 1 ชั่วโมง, PM2.5
- ตารางนัดหมายวันนี้จากฐานข้อมูลนัดหมายของระบบ
- ปุ่ม `แก้ไข` / `ลบ` สำหรับนัดหมายแต่ละรายการ
- คำแนะนำแยกคนเมือง/ชนบทตามข้อมูลจริงที่ระบบได้รับ
- คำลงท้าย: `ขอให้นายท่านเดินทางโดยปลอดภัยนะคะ`

ตัวอย่างข้อความ:

```text
เรียน นายท่าน วันนี้ดิฉัน SmartLife ขอรายงานดังนี้ค่ะ

🌤 อุณหภูมิสูงสุด: 32°C
🌧 โอกาสฝน 12 ชม.: 60%
☔ ฝนล่าสุด 1 ชม.: 0 มม.
💨 PM2.5: 35 µg/m³

📋 ตารางงานวันนี้:
- ประชุมทีม SmartLife เวลา 10:00 น.
- นัดหมายกับลูกค้า เวลา 14:00 น.

[ปุ่ม: แก้ไข] [ปุ่ม: ลบ]

คนเมือง: เผื่อเวลาเดินทางและเตรียมร่ม
ชนบท: เช็กงานกลางแจ้งและทางดินที่อาจลื่น

ที่มา: OpenWeather เวลา 29 พ.ค. 2569 06.00 น.
ขอให้นายท่านเดินทางโดยปลอดภัยนะคะ
```

## คำถามเรื่องอากาศ

นอกจากรายงานอัตโนมัติทุกเช้าเวลา 06:00 ผู้ใช้ยังสามารถพิมพ์ถามเรื่องอากาศได้โดยตรง เช่น:

```text
วันนี้ฝนจะตกไหม
วันนี้อุณหภูมิสูงสุดเท่าไร
วันนี้ฝุ่นระดับใด
วันนี้ PM2.5 เท่าไร
วันนี้หนาวไหม
วันนี้ร้อนไหม
```

หรือส่งโลเคชันจาก LINE เพื่อให้ระบบรายงานจากพิกัดนั้นโดยตรง

ระบบจะตอบโดยใช้ข้อมูลล่าสุดจาก OpenWeather และ Air Pollution API หากข้อมูลบางรายการไม่มี ระบบจะแจ้งว่า `ยังไม่มีข้อมูล` แทนการเดาค่าขึ้นเอง

## รายงานที่คงไว้

ระบบยังคงรายงานภาวะฉุกเฉินและเร่งด่วน โดย alert ทั่วไปยังเน้นประเทศไทย ส่วนแผ่นดินไหว พายุ และสึนามิครอบคลุมอาเซียน

ระบบจะ sync เหตุสดจาก USGS/GDACS เข้า collection `alerts` ทุก 15 นาที แล้วตรวจทุก 1 นาทีเพื่อส่ง LINE เมื่อพบ alert ที่ยัง active และยังไม่เคยส่งแจ้งเตือนให้ผู้ใช้นั้น

เกณฑ์ sync สดอัตโนมัติ:

- แผ่นดินไหวในอาเซียนจาก USGS ขนาด M5.0 ขึ้นไป หรือมี tsunami flag
- พายุ/สึนามิในอาเซียนจาก GDACS เฉพาะระดับสีส้ม/แดง
- รายการซ้ำจะอัปเดต record เดิมด้วย `externalId` เพื่อไม่ส่งซ้ำให้ผู้ใช้เดิม

ระบบจะเตือนภัยธรรมชาติสำคัญเหล่านี้ด้วย:

- ภัยพิบัติ: `disaster`, `natural_disaster`, `ภัยพิบัติ`
- พายุในอาเซียน: `storm`, `thunderstorm`, `typhoon`, `cyclone`, `tropical_storm`, `พายุ`
- แผ่นดินไหวในอาเซียน: `earthquake`, `quake`, `แผ่นดินไหว`
- สึนามิในอาเซียน: `tsunami`, `tidal_wave`, `สึนามิ`, `สึมามิ`, `คลื่นสึนามิ`
- น้ำท่วม: `flood`, `flooding`, `flash_flood`, `น้ำท่วม`

ข้อความแผ่นดินไหวจะรายงานเฉพาะข้อมูลหลัก: เกิดที่ไหน, ขนาดกี่ริกเตอร์, ไกลจาก กทม. กี่กิโลเมตร และอาการที่รับรู้ได้ พายุและสึนามิใช้หลักเดียวกัน โดยไม่ใส่ถ้อยคำให้หลบภัยหรืออพยพในข้อความ LINE เพื่อลดความตื่นตระหนก

ผู้ใช้สามารถขอดูรายงานฉุกเฉินเองได้โดยพิมพ์:

```text
ภาวะฉุกเฉิน
```

หรือ:

```text
เร่งด่วน
```

หรือ:

```text
ภัยพิบัติ
```

หรือเลือกดูเฉพาะประเภท:

```text
พายุ
แผ่นดินไหว
สึนามิ
น้ำท่วม
```

ระบบยังคงแจ้งเตือนก่อนกิจกรรมตามระยะเวลาเดิม:

- ก่อนกิจกรรม 1 วัน
- ก่อนกิจกรรม 6 ชั่วโมง
- ก่อนกิจกรรม 1 ชั่วโมง
- ก่อนกิจกรรม 30 นาที

ข้อความเตือนก่อนกิจกรรมจะมีหัวข้อ เวลา สถานที่ สิ่งที่ต้องเตรียม และปุ่ม quick reply สำหรับ `แก้ไข` / `ลบ`

ระบบยังคงรายงานตามที่ผู้ใช้ขอดู โดยพิมพ์:

```text
รายงาน
```

หรือ:

```text
รายงานวันนี้
```

หรือ:

```text
ตารางวันนี้
```

ระบบจะแสดงตารางนัดหมายวันนี้ พร้อม ID สำหรับแก้ไขหรือลบรายการ

## Environment

คัดลอก `.env.example` เป็น `.env` แล้วใส่ค่าจริงก่อนรันระบบ

```bash
MONGODB_URI=mongodb://localhost:27017/smartlife
MONGODB_DNS_SERVERS=8.8.8.8,1.1.1.1
LINE_ACCESS_TOKEN=...
LINE_USER_ID=...
WEATHER_API_KEY=...
```

## ตรวจเสถียรภาพระบบ

รันชุดตรวจหลัก:

```bash
npm test
```

ชุดตรวจนี้ครอบคลุม syntax, timezone ประเทศไทยแบบ 24 ชั่วโมง, parser แก้/ลบนัดหมาย, การกันนัดหมายซ้ำ, การเลือก reminder ที่ควรส่งจริงโดยไม่สแปม, และรูปแบบ alert แผ่นดินไหว/สึนามิแบบไม่สร้างความตื่นตระหนก

หลังเปิด server แล้วตรวจสถานะ runtime ได้ที่:

```text
GET /health
```

ระบบจะตอบสถานะ MongoDB, timezone `Asia/Bangkok`, และเวลาประเทศไทยปัจจุบัน

## สมอง AI ใน LINE

ระบบรองรับการต่อสมอง AI แบบเลือก provider ได้ โดยยังแยกระบบนัดหมายออกจาก AI เพื่อไม่ให้ AI เดาหรือแก้นัดหมายผิดเอง

คำสั่งใน LINE:

```text
สถานะ AI
แปล วันนี้ฉันมีประชุมตอน 15.00 น.
สร้างภาพ ภาพคนเมืองตรวจฝุ่นก่อนออกจากบ้าน สไตล์อินโฟกราฟิก
```

การตั้งค่าใน `.env`:

```bash
AI_PROVIDER=auto
AI_FALLBACK_PROVIDERS=gemini,groq,openrouter,huggingface,pollinations
GEMINI_API_KEY=...
GEMINI_MODEL=gemini-2.5-flash
AI_MAX_HISTORY=12

# ทางเลือกอื่นสำหรับแชต/แปล
GROQ_API_KEY=...
GROQ_MODEL=llama-3.1-8b-instant
OPENROUTER_API_KEY=...
OPENROUTER_MODEL=openrouter/free
HF_TOKEN=...
HF_MODEL=deepseek-ai/DeepSeek-V3-0324
HF_IMAGE_MODEL=black-forest-labs/FLUX.1-dev
POLLINATIONS_API_KEY=...
POLLINATIONS_TEXT_MODEL=openai
POLLINATIONS_IMAGE_MODEL=flux
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# สร้างภาพใน LINE ต้องมี URL สาธารณะ HTTPS ให้ LINE โหลดรูปได้
IMAGE_PROVIDER=pollinations
IMAGE_FALLBACK_PROVIDERS=pollinations,openai,huggingface
OPENAI_API_KEY=...
OPENAI_IMAGE_MODEL=gpt-image-1-mini
PUBLIC_BASE_URL=https://your-public-domain.example.com
LIFF_ID=your_liff_id
```

ตัวเลือกที่แนะนำ:

- `Gemini` เหมาะเป็นค่าเริ่มต้นสำหรับแชตและแปลภาษาไทย เพราะมี free tier และโควตาเริ่มต้นเพียงพอสำหรับทดสอบ
- ถ้าต้องการลองรุ่นใหม่ของ Gemini ให้เปลี่ยน `GEMINI_MODEL` เป็นรุ่น preview ที่บัญชีรองรับ เช่น `gemini-3-flash-preview`; ถ้าเน้นเสถียรให้ใช้ `gemini-2.5-flash`
- `Groq` เหมาะเมื่ออยากได้คำตอบเร็วและมี free plan แต่ต้องดู rate limit ของบัญชีจริง
- `OpenRouter` ตั้ง `OPENROUTER_MODEL=openrouter/free` เพื่อให้ OpenRouter เลือกโมเดลฟรีที่ยังใช้ได้ให้อัตโนมัติ
- `Pollinations` ใช้ key ฟรีจาก `https://enter.pollinations.ai` และ key เดียวใช้ได้ทั้งแชตกับสร้างภาพ
- `Ollama` เหมาะเมื่ออยากรันฟรีในเครื่อง ไม่ส่งข้อความออกนอกเครื่อง แต่ต้องมีเครื่องที่แรงพอ และควรเพิ่ม `ollama` ใน `AI_FALLBACK_PROVIDERS` เฉพาะตอนเปิด Ollama แล้ว
- `Hugging Face Inference Providers` เหมาะสำหรับทดลองหลายโมเดลและใช้ token เดียวผ่าน router ของ Hugging Face
- `Hugging Face Image` เป็นทางเลือกสร้างภาพผ่าน free tier เมื่อมี `HF_TOKEN`, `HF_IMAGE_MODEL`, และ `PUBLIC_BASE_URL`
- `OpenAI Images` ใช้เฉพาะสร้างภาพ ต้องมี key และ public HTTPS URL; ถ้าต้องการคุณภาพสูงกว่าค่าเริ่มต้นให้ตั้ง `OPENAI_IMAGE_MODEL=gpt-image-2`

เมื่อใช้ `AI_PROVIDER=auto` ระบบจะลองตามลำดับใน `AI_FALLBACK_PROVIDERS` เฉพาะตัวที่ตั้งค่า key ครบ ถ้าตัวแรกหมดโควตา ล่ม หรือ timeout จะข้ามไปตัวถัดไปโดยอัตโนมัติ
ค่าเริ่มต้นของระบบสร้างภาพใช้ `IMAGE_PROVIDER=pollinations` เพื่อให้ใช้งานได้แม้ยังไม่มี OpenAI หรือ Hugging Face key
